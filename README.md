# Data-Structures-Algorithms
This script represents a warehouse floor plan as a Graph (using a Python dictionary) and uses the Breadth-First Search (BFS) algorithm to find the shortest path between inventory zones.
